{
  "name": "shell-commands-server",
  "version": "1.0.0",
  "description": "Node server for running Shell commands",
  "main": "server.js",
  "scripts": {
    "preinstall": "npm install pm2 bun -g",
    "start": "pm2 start src/server.ts & pm2 save & pm2 startup"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.1"
  }
}
